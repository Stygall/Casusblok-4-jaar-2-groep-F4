import React from "react";
import { OnAccountChanged } from "../../hooks/useMoralis/_useMoralisAuth";
interface MoralisProviderOptions {
    onAccountChanged?: OnAccountChanged;
}
export interface MoralisProviderProps {
    children: React.ReactNode;
    appId: string;
    serverUrl: string;
    jsKey?: string;
    dangerouslyUseOfMasterKey?: string;
    options?: MoralisProviderOptions;
}
export declare const MoralisProvider: ({ children, appId, jsKey, dangerouslyUseOfMasterKey, serverUrl, options: { onAccountChanged }, }: MoralisProviderProps) => JSX.Element;
export {};
