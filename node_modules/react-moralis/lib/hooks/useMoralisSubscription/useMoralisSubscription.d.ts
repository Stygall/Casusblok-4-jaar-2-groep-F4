import { Moralis } from "moralis";
import { Query } from "src/utils/genericQuery";
import { MoralisListenerHandler } from "./_useMoralisSubscriptionListener";
export interface UseSubscriptionQueryOptions {
    enabled?: boolean;
    onCreate?: MoralisListenerHandler;
    onUpdate?: MoralisListenerHandler;
    onEnter?: MoralisListenerHandler;
    onLeave?: MoralisListenerHandler;
    onDelete?: MoralisListenerHandler;
}
export declare const useMoralisSubscription: <Entity extends Moralis.Attributes = Moralis.Attributes>(nameOrObject: string | Moralis.Object, queryMap?: (q: Query<Entity>) => Query<Entity>, dependencies?: any[], options?: UseSubscriptionQueryOptions) => void;
