name: development test 2
on: [push]
jobs:
  launch_moralis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set Node.js 10.x
        uses: actions/setup-node@master
        with:
          node-version: '14'
      - uses: actions/checkout@v2
      - name: Set Node.js 10.x
        uses: actions/setup-node@master
        with:
          node-version: '14'
      - name: find directory
        run: ls
      - name: install node modules
        working-directory: ruilwinkel
        run: npm install
      - name: install surge
        working-directory: ruilwinkel
        run: npm install --global surge
      - name: build front end
        shell: bash
        working-directory: ruilwinkel
        run: npm run build     
      - name: surge frontend
        shell: bash
        working-directory: ruilwinkel
        SURGE_LOGIN: 1959514smeets@zuyd.nl
        SURGE_TOKEN: ff1bc4dc2eb1c16f9f5c4e8e7cb38cbf
        run: surge --project ./build --domain ruilwinkel.surge.sh 
