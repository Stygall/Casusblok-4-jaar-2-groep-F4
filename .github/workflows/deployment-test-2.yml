name: development test 2
on: [push]
jobs:
  launch_moralis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set Node.js 10.x
        uses: actions/setup-node@master
        with:
          node-version: '14'
      - name: install node modules
        working-directory: ruilwinkel
        run: npm install
      - name: install moralis admin cli
        working-directory: ruilwinkel
        run: npm install -global moralis-admin-cli
      - name: run frontend
        shell: bash
        working-directory: ./ruilwinkel/src
        run: moralis-admin-cli deploy --moralisApiKey z4VED4PyG39Zpvv --moralisApiSecret 67e0A5yLw8xEpJw --d 2l8hm6ropitg.moralis.io --p 

